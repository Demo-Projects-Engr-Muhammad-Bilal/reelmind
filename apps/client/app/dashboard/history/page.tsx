import { SharedReelView } from "@/components/dashboard/Studio/SharedReelView";

export default function HistoryPage() {
          return (
                    <SharedReelView
                              title="My History"
                              description="Your successfully completed and rendered AI generated reels."
                              pageType="history"
                    />
          );
}